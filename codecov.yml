coverage:
  ignore:
    - "test_data/**/*"
  status:
    project:
      default:
        target: 80%
        threshold: 1%
    patch:
      default:
        target: 80%
        threshold: 5%

comment:
  layout: "diff,flags,tree"
  behavior: default
  require_changes: true